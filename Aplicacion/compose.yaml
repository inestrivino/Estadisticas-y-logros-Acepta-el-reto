services:
  app:
    build: .
    ports:
      - "3000:3000"
      - "8080:8080"
      - "9229:9229"
    depends_on:
      - redis
    volumes:
      - .:/app
      - /app/node_modules
  
  redis:
    image: "redis"
    ports:
      - "6379:6379"